# План: перенос sentgraph на TypeScript и раскатка памяти на всех агентов

Составлен 14.08.2026 по протоколу `bito-build-plan` (разведка через AI Architect —
8 запросов; внешний ресёрч — 6 агентов; замеры на этой машине). Уровень: Large.

## 1. Решение и рамки

**Новый закрытый репозиторий** на TypeScript; текущий Go-репозиторий остаётся
рабочим до приёмки и выводится из эксплуатации последним шагом.

Что решено (владельцем, по ходу планирования):

- Новый репозиторий называется **`zmem`**; так же называется и CLI — короткое имя
  вместо `sentgraph-mcp`, печатать его придётся часто.
- Распространение — **pnpm из git**, не Homebrew. Реестр npm не нужен вообще:
  `pnpm add -g git+ssh://git@github.com/stashilin/zmem.git#vX.Y.Z`.
  SSH-доступ к GitHub работает, и *существующий* клон маркетплейса имеет
  SSH-remote (`git@github.com:...`). Но в реестре источник записан как `github`,
  и установка плагина из **закрытого** репозитория с нуля пока НЕ проверена —
  это задача WS0.5, и на ней стоит вся схема распространения.
- **Бинарь не в приоритете**, компиляция в standalone не делается.
- **Холодный старт не учитывается как фактор** (решение владельца, зафиксировано
  явно). Замеры сохранены в §7 как контекст, а не как аргумент.
- Форма: **библиотека + тонкие CLI** (вариант «Б»). Ядро публикуется как
  импортируемый модуль; CLI, плагины и будущие прод-агенты — его потребители.

Что умирает вместе с Homebrew: tap `stashilin/homebrew-tap`, cask, GoReleaser,
секрет `HOMEBREW_TAP_GITHUB_TOKEN`, вся возня с подписью macOS.

## 2. Целевая архитектура

```
zmem — закрытый репозиторий, pnpm-монорепо
│
├── packages/core        @zmem/core — вся логика, единственная копия
│   ├── config           env-first + .env.local в директории проекта
│   ├── redact           вырезание секретов ДО любой отправки
│   ├── memory           Service: лимиты, чанки, роутинг user/project
│   └── zepstore         адаптер @getzep/zep-cloud за интерфейсом Gateway
│
├── packages/cli         zmem — serve | hook | doctor | ingest
│
├── packages/amp-plugin  agent.start / agent.end, импортирует core напрямую
│
└── plugin/              плагин Claude Code (hooks.json + скилы), маркетплейс
```

Ключевое отличие от нынешней схемы: **плагины Amp и omp импортируют ядро как
модуль**, а не запускают процесс. Отдельный вход `ingest` нужен только Codex —
у него хуки это внешние процессы.

## 3. Workstreams и задачи

Оценки даны парой: **вручную** / **агентами** (та же работа на агентной
платформе с ревью человеком).

### WS0. Страховка перед началом (делать первым)

Артефакты 0.2–0.3 складываются в текущий Go-репозиторий (`testdata/`) и
переносятся в `zmem` при создании — нового репозитория на этом шаге ещё нет.

| # | Задача | Оценка | Критерий приёмки |
|---|---|---|---|
| 0.1 | В Go-репозиторий добавить тест redact на **два секрета одного вида подряд** | 0.5д / 0.2д | Тест падает на наивной JS-реализации без `/g`; в Go зелёный |
| 0.2 | Выгрузить золотые векторы redact в `testdata/redact-golden.json` (вход → ожидаемый выход), включая кириллицу, эмодзи, длинные тексты | 0.5д / 0.2д | Покрыты все 8 паттернов; файл закоммичен в Go-репозиторий |
| 0.3 | Выгрузить золотые векторы обрезки и чанкинга (границы 4096 и 10000 символов на кириллице/эмодзи) | 0.5д / 0.2д | Для каждого входа зафиксирована длина в кодовых точках |
| 0.4 | **Контракт имён окружения**: решить `SENTGRAPH_*` против `ZMEM_*` | 0.2д / 0.1д | Решение записано в план; при переименовании — core читает старые имена как алиасы, иначе миграция запрещена |
| 0.5 | **Проверить приватный маркетплейс**: тестовый закрытый репозиторий с `.claude-plugin/marketplace.json`, `plugin marketplace add` + `install --scope project` | 0.3д / 0.2д | Плагин ставится и хук стартует; при отказе зафиксирована альтернатива |

Почему первым: после удаления Go-кода векторы невосстановимы, а именно они
ловят тихие регрессии безопасности (§6). 0.4 идёт до кода, потому что
`SENTGRAPH_PROJECT_ID` напрямую формирует `graph_id` (`proj:` + значение):
переименование переменной или смена значения расщепляет уже накопленные
`proj:aggr` и `proj:selfenv` на новые пустые графы. 0.5 — потому что на
установке плагина из закрытого репозитория стоит вся схема распространения, а
проверено пока лишь то, что *существующий* клон имеет SSH-remote; добавление
приватного источника с нуля не проверялось.

### WS1. Каркас нового репозитория

| # | Задача | Оценка | Критерий приёмки |
|---|---|---|---|
| 1.1 | Создать закрытый репозиторий, pnpm-монорепо, TypeScript, Vitest, Biome; версии через mise (конвенции из paynexus) | 0.5д / 0.2д | `pnpm i && pnpm test` проходит на чистом клоне |
| 1.2 | `prepare`-скрипт сборки, чтобы установка из git-URL собирала пакет | 0.5д / 0.3д | `pnpm add -g git+ssh://…` на чистой машине даёт рабочий `sentgraph-mcp` |
| 1.3 | CI на GitHub Actions: typecheck + Vitest + Biome на PR | 0.5д / 0.2д | Красный CI блокирует merge |

### WS2. Ядро `@sentgraph/core`

| # | Задача | Оценка | Критерий приёмки |
|---|---|---|---|
| 2.1 | `redact` + прогон золотых векторов из 0.2 | 1д / 0.5д | Все векторы совпадают байт в байт; тест «два секрета одного вида» зелёный |
| 2.2 | Утилиты длины и обрезки **по кодовым точкам** (`Array.from`, не `length`) + векторы из 0.3 | 1д / 0.5д | Границы совпадают с Go на кириллице и эмодзи |
| 2.3 | `config`: env-first, `.env.local` только в директории проекта, `ErrProjectNotConfigured` | 1д / 0.5д | Портированы все тесты Go, включая гейт и битый файл |
| 2.4 | `zepstore` на `@getzep/zep-cloud` за интерфейсом `Gateway` | 1.5д / 0.7д | Девять методов; ensure-паттерн ловит 404 через `ZepError.statusCode` |
| 2.5 | `memory.Service`: лимиты 30/4096/10000, чанкинг с метаданными, роутинг user/project | 1.5д / 0.7д | Портированы тесты на фейковом Gateway |

### WS3. CLI `sentgraph-mcp`

| # | Задача | Оценка | Критерий приёмки |
|---|---|---|---|
| 3.1 | `serve` на `@modelcontextprotocol/server@2`, шесть тулзов | 1.5д / 0.7д | `tools/list` совпадает со схемами Go; хендлеры возвращают И `content`, И `structuredContent` |
| 3.2 | `hook <event>` — пять событий Claude Code, парсер JSONL-транскрипта, защита путей | 1.5д / 0.7д | Портированы тесты hooks и transcript |
| 3.3 | `doctor [--online]` | 0.3д / 0.1д | Вывод совпадает с нынешним |
| 3.4 | **`ingest`** — агентно-независимый вход: JSON-сообщения на stdin, redact и роутинг внутри | 1д / 0.5д | Codex-адаптер пишет ход в память; запись видна поиском |

### WS4. Адаптеры агентов

| # | Задача | Оценка | Критерий приёмки |
|---|---|---|---|
| 4.1 | Codex: ветка разбора его пейлоада (Stop несёт `last_assistant_message` в stdin, транскрипт не парсим), записи в `~/.codex/hooks.json` | 1д / 0.5д | Живая сессия Codex: записал → нашёл |
| 4.2 | Amp-плагин: `agent.end` → запись, `agent.start` → впрыск; импорт ядра напрямую | 1.5д / 0.7д | `amp plugins exec` на синтетических событиях + живая сессия |
| 4.2a | **Вырезание собственного впрыска**: помечать впрыснутый в `agent.start` блок маркером и убирать его перед записью хода; запись неблокирующая (disposal хендлера ~3 с) | 0.5д / 0.3д | После трёх ходов в графе нет эпизода, содержащего ранее впрыснутый блок памяти |
| 4.3 | Плагин Claude Code переезжает в новый репозиторий (hooks.json + пять скилов) | 0.5д / 0.2д | Установка из нового маркетплейса; **живой ход записан и найден поиском**, а не только прочитан контекст |
| 4.4 | omp — за владельцем; от нас стабильный вход (`ingest` либо импорт ядра) | — | — |

### WS5. Миграция и вывод старого

| # | Задача | Оценка | Критерий приёмки |
|---|---|---|---|
| 5.1 | Раскатка в шесть проектов из нового источника | 0.5д / 0.2д | В каждом проекте: **живой ход записан и найден `memory_search` в графе проекта** (проверять на Stop, а не только SessionStart); `doctor` печатает **тот же graph_id**, что до миграции |
| 5.2 | **Снос старых записей** — отдельная задача, после приёмки 5.1 | 0.5д / 0.3д | `which sentgraph-mcp` пуст; `grep -ril sentgraph` по `~/.claude`, `~/.codex`, `~/.config/amp` и шести проектам не даёт исполняемых записей |
| 5.3 | Снести Homebrew tap, cask, GoReleaser, workflow релиза | 0.3д / 0.1д | `brew list --cask` пуст; мёртвых каналов установки не осталось |
| 5.4 | Архивировать Go-репозиторий (не удалять) — **только после решения по backfill** | 0.2д / 0.1д | README указывает на новый репозиторий; backfill либо перенесён, либо оставлен рабочим |

Инвентарь для 5.2 (найдено на диске): клон маркетплейса
`~/.claude/plugins/marketplaces/sentgraph-mcp/`, `known_marketplaces.json`,
`extraKnownMarketplaces` в `~/.claude/settings.json`, шесть записей в
`installed_plugins.json`, два кэша плагина (0.4.0 и 0.5.0), пять проектных
`settings.local.json` с `enabledMcpjsonServers`, корневой `.mcp.json`,
`[mcp_servers.sentgraph]` в `~/.codex/config.toml`, `mcpTrustedServers` в
`~/.config/amp/settings.json`, симлинк `/opt/homebrew/bin/sentgraph-mcp`.
Пока старый бинарь остаётся на PATH, его хуки пишут в те же графы параллельно
с `zmem` — это и есть двойная запись.

**Итого:** ≈ 17–19 дней вручную, ≈ 8–9 агентских.

## 4. Последовательность

```
WS0 (страховка)
  └─▶ WS1 (каркас) ─▶ WS2 (ядро) ─┬─▶ WS3 (CLI) ─▶ WS4.1 Codex ─┐
                                   └─▶ WS4.2 Amp ───────────────┼─▶ WS5
                                       WS4.3 плагин CC ─────────┘
```

WS0 — единственный жёсткий барьер: без золотых векторов остальное стартовать
нельзя. Внутри WS2 задачи 2.1–2.2 идут до 2.4–2.5. Adapters (4.1–4.3)
параллельны друг другу.

**Альтернативный порядок, если память нужна у остальных агентов раньше:**
сделать `ingest` и Codex-адаптер в текущем Go-репозитории (2–3 дня), а порт
начать после. Цена: `ingest` придётся написать дважды.

## 5. Сквозные вопросы

**Паритет, а не «переписал и ладно».** До удаления Go-кода обе реализации
прогоняются на одном входе: золотые векторы redact и обрезки, плюс сравнение
выхода `hook SessionStart` и `tools/list` байт в байт.

**Приёмка — измеренная память, а не конфиг.** Для каждого агента: записал ход →
нашёл его поиском в том же графе. Урок 13.08: sentgraph месяц выглядел
установленным в пяти проектах и не писал ни байта.

**Откат.** Go-репозиторий и рабочая установка v0.5.0 остаются нетронутыми до
конца WS5; откат — переустановка плагина из старого маркетплейса и `brew
install --cask`.

**Секреты.** `.env.local` в каждом проекте (уже созданы). Учесть: рантайм сам
подхватывает `.env`/`.env.local` из cwd — гейт `RequireProjectConfig` обязан
читать файл сам, а не полагаться на автозагрузку, иначе чужой `.env` молча
настроит посторонний каталог.

**Наблюдаемость.** `doctor` остаётся единственной командой проверки; в него
добавить, откуда пришла конфигурация (env или файл) — сейчас этого не видно, а
после смены контракта это первый вопрос при разборе.

## 6. Риски

| Риск | Вероятность | Влияние | Митигация | Ранний признак |
|---|---|---|---|---|
| `String.replace` без `/g` заменит только первый секрет → утечка в Zep | Высокая | **Критическое** | WS0.1 + золотые векторы; ревью каждого паттерна | Тест «два секрета одного вида» падает |
| Обрезка по `length` (UTF-16) вместо кодовых точек → расхождение лимитов на кириллице | Высокая | Среднее | WS2.2 + векторы границ | Длина в тесте не совпадает с Go |
| Пустая строка вместо `nil` уедет в Zep (`name`, `createdAt`, `sourceDescription`) | Средняя | Среднее | `|| undefined` в трёх местах, тест на сериализацию | Поле появилось в запросе |
| TS MCP SDK не добивает `content[]` из `structuredContent` | Высокая | Высокое | Возвращать оба поля в каждом хендлере | Claude Code видит пустой ответ |
| Инлайн-флаги `(?i)`/`(?s)` из Go не компилируются в JS | Высокая | Низкое (шумно падает) | Флаги конструктора при переносе | SyntaxError на старте |
| Бэктрекинг JS-регулярок (в Go RE2 его нет) на длинных текстах бэкфилла | Средняя | Среднее | Тест на 20k символов без пунктуации | Время чанкинга растёт нелинейно |
| Дубли эпизодов из-за ретраев SDK на write-путях | Средняя | Среднее | `maxRetries: 0` на записи, манифест `last_event_at` | Повторы в графе после сбоя сети |
| **Расщепление графа** при смене имени/значения `SENTGRAPH_PROJECT_ID` | Средняя | **Критическое** | WS0.4 (контракт имён, алиасы) + критерий 5.1 «тот же graph_id» | `doctor` печатает другой `proj:` |
| **Двойная запись**: старый бинарь остаётся на PATH и его хуки живут параллельно | Высокая | Высокое | WS5.2 как отдельная задача с чек-листом | Каждый ход в графе дважды |
| **CLI не резолвится в окружении хука** (иной PATH, нет node) | Высокая | Высокое | Зафиксировать способ вызова в WS1; проверка `env -i PATH=/usr/bin:/bin zmem hook SessionStart` отвечает JSON | Хук молча ничего не делает |
| Amp пишет в память собственный впрыснутый контекст (рекурсия) | Высокая | Высокое | WS4.2a — маркер и вырезание | Эпизоды содержат блок «User memory:» |

## 7. Замеры: не фактор решения, но три инженерных следствия

Владелец вывел скорость старта из факторов — раздел оставлен ради решений
по реализации, а не как аргумент за или против переноса.

Измерено на этой машине (n=50 на вариант, пол fork/exec 1.99 мс): нынешний
Go-хук 3.81 мс; сборка «pnpm-шим → mise → node → статический SDK» — 54.99 мс.
При таймауте хука 20 секунд это доли процента бюджета, но три вещи стоит
заложить в реализацию сразу, потому что потом их дороже переделывать:

1. **Ленивый импорт Zep SDK.** Если импорт не выполняется, он стоит +0.96 мс;
   если выполняется — +44 мс. Путь «проект не настроен» и путь чтения
   контекста не должны трогать SDK вовсе. Задача 3.2 принимает это как
   требование.
2. **Собирать CLI бандлом** (esbuild или аналог): 448 CJS-модулей SDK против
   одного файла — 69.7 мс против 35.9 мс. Но бандл ухудшает *ленивый* путь на
   4.2 мс, поэтому либо бандлить с сохранением динамического импорта, либо
   осознанно выбрать одно из двух.
3. **Никакого `npx` в хуках.** Он стоит 491–512 мс на вызов даже с точным
   пином версии (пин не отменяет похода в реестр). Известный прецедент —
   ruflo#1530, где это дало 18–21 секунду на промпт. Установка только через
   `pnpm add -g`, вызов — по имени команды.

Побочный факт на будущее: если бы понадобилось оставить ядро на Go и всё равно
раздавать через pnpm, это рабочий путь — паттерн `optionalDependencies` с
платформенными пакетами (как у turbo и biome, без postinstall-скачивания);
кросс-собранные Go-бинари подписываются линкером ad-hoc и не карантинятся.
Цена — 11–15 мс из-за обязательного шима.

## 8. Открытые вопросы

| # | Вопрос | Что блокирует | Кто решает |
|---|---|---|---|
| 1 | Порядок: полный порт или сначала `ingest`+Codex на Go | Старт WS3.4/WS4.1 | владелец |
| 2 | Судьба `scripts/backfill` (1147 строк: Batch API, два источника, манифест, dry-run) — переносить или оставить рабочим в отдельном репозитории | **WS5.4** (архивация раньше решения = потеря возможности переливать день и доки; манифесты `.ingest/` станут нечитаемы) | владелец |
| 3 | omp-адаптер делает владелец; нужен ли ему `ingest` или импорт ядра | Форма публичного API ядра | владелец |
| 4 | Имена переменных окружения (см. WS0.4) — оставить `SENTGRAPH_*` или ввести `ZMEM_*` с алиасами | WS2.3 и вся раскатка | владелец |

Имя решено: репозиторий и CLI — `zmem`. Переименование команды затрагивает
`hooks.json` плагина, `.mcp.json`, записи в `~/.codex/hooks.json` и все шесть
проектов; это часть WS5.1, отдельной задачи не требует, но при раскатке старые
и новые записи не должны сосуществовать — иначе память будет писаться дважды.

## 9. Context Handoff (for downstream skills)

- **Репозиторий (источник):** `stashilin/sentgraph-mcp`, проиндексирован в AI
  Architect 14.08.2026, состояние In-Sync.
- **Ближайший аналог для порта:** сам Go-код; карта файлов и размеров —
  `getFieldPath(sentgraph-mcp, file_structures)`.
- **Инварианты Zep (подтверждены доменным знанием):** лимиты в символах, не
  байтах — 30 сообщений/вызов, 4096 симв/сообщение, 10000 симв/эпизод, 350
  items/`batch.add`, 50000/batch; метаданные ≤ 10 ключей; `created_at`
  определяет временную валидность фактов.
- **Инвариант проекта:** `redact` применяется до любой отправки; граф проекта —
  `proj:` + `SENTGRAPH_PROJECT_ID`, один проект может охватывать несколько
  репозиториев.
- **Соглашения:** документация и коммиты на русском, идентификаторы на
  английском; конвенции стека берутся из paynexus (Zod, Vitest, Biome, mise).
- **Отложено (не в объёме):** прод-агенты paynexus подключат то же ядро, но
  это отдельная работа; omp-адаптер за владельцем.
